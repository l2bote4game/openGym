services:
  - type: web
    name: opengym
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "10000"
      - key: DATA_DIR
        value: /tmp/data
      - key: RP_NAME
        value: openGym
